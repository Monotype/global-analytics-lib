{
  "name": "global-analytics-lib",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "VITE_APP_VERSION=$(node -p \"require('./package.json').version\") vite build && esbuild dist/$(node -p \"require('./package.json').version\")/global-analytics-lib.js --bundle --minify --outfile=dist/$(node -p \"require('./package.json').version\")/global-analytics-lib.min.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
