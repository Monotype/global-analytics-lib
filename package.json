{
  "name": "global-analytics-lib",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild dist/global-analytics-lib.js --bundle --minify --outfile=dist/global-analytics-lib.min.js",
    "build:pre-prod" : "vite build --mode development",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
